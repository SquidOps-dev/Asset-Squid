<Window x:Class="SquidOps_AssetSquid.Views.AddLocationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Location"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="Black"
        ResizeMode="NoResize"
        Icon="../Assets/favicon.ico">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add New Location"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="DeepSkyBlue"
                   Margin="0,0,0,20" />

        <StackPanel Grid.Row="1" VerticalAlignment="Top">
            <TextBlock Text="Location Name" Foreground="LightGray" FontSize="14"/>
            <TextBox x:Name="LocationNameBox" Margin="0,10" Height="26" FontSize="14"/>
            <Button Content="Save"
                    Height="36"
                    Background="#1f8ea3"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
